# 短视频功能数据库与接口说明

本文档用于短视频功能的数据库建表、后端接口联调和前端接入说明。当前功能目标是：用户上传视频后先进入私人库，只在个人资料页可见；用户主动发布公开后，其他账号访问首页短视频流时可以看到该视频。

## 一、业务流程

### 1. 上传到私人库

用户在前端选择本地视频后，前端通过 `uni.uploadFile` 上传到后端：

```text
POST /api/videos/upload
```

后端执行：

```text
1. 接收前端上传的视频文件
2. 保存到服务器 uploads/videos/ 目录
3. 生成 video_url，例如 /uploads/videos/20260708120000_xxx.mp4
4. 向 training_video_record 表插入一条记录
5. 默认 visibility=private，status=uploaded
6. 返回视频记录给前端
```

此时视频只属于上传用户自己，个人资料页可见，首页短视频流不可见。

### 2. 我的私人视频库

个人资料页查询当前用户自己的视频：

```text
GET /api/videos/my?user_id=1
```

查询条件：

```text
user_id = 当前用户
status != deleted
```

返回结果包含私密视频和已公开视频。

### 3. 发布到公开短视频流

用户点击“发布公开”后，前端调用：

```text
POST /api/videos/{video_id}/publish
```

后端执行：

```text
visibility = public
status = published
publish_time = 当前时间
```

发布后，首页短视频流可查询到该视频，其他账号也可以看到。

### 4. 首页短视频流

首页调用：

```text
GET /api/feed/list?page=1&page_size=10
```

首页只展示公开视频，查询条件：

```text
visibility = public
status = published
```

如果数据库没有公开视频，或数据库暂时不可用，后端会继续使用原来的静态视频兜底，避免首页空白。

## 二、视频文件存储原则

数据库不直接存储视频二进制内容，只存储视频访问路径和业务字段。

推荐方式：

```text
视频文件：服务器磁盘或对象存储
数据库：video_url、cover_url、user_id、visibility、status 等元数据
```

当前代码默认保存目录：

```text
uploads/videos/
```

后端主服务已挂载静态资源：

```python
app.mount("/uploads", StaticFiles(directory="uploads"), name="uploads")
```

因此视频访问地址格式为：

```text
http://服务器IP:9000/uploads/videos/xxx.mp4
```

## 三、核心数据表

核心表为：

```text
training_video_record
```

完整建表 SQL 文件：

```text
tennis0.7/tennis0.7/TennisCoach（后端代码）/sql/training_video_record.sql
```

如果服务器上已经存在旧版 `training_video_record` 表，请执行增量迁移文件：

```text
tennis0.7/tennis0.7/TennisCoach（后端代码）/sql/training_video_record_video_feed_migration.sql
```

## 四、字段说明

| 字段名 | 类型 | 是否必需 | 默认值 | 说明 |
|---|---:|---:|---:|---|
| id | BIGINT | 是 | 自增 | 主键 ID |
| user_id | BIGINT | 是 | 无 | 上传用户 ID |
| title | VARCHAR(120) | 否 | NULL | 视频标题 |
| description | TEXT | 否 | NULL | 视频描述或发布文案 |
| video_url | VARCHAR(500) | 是 | 无 | 视频访问路径，例如 `/uploads/videos/xxx.mp4` |
| cover_url | VARCHAR(500) | 否 | NULL | 视频封面路径，当前可为空 |
| duration_seconds | DOUBLE | 否 | NULL | 视频时长，单位秒，当前可为空 |
| file_size | BIGINT | 否 | NULL | 文件大小，单位字节 |
| source_type | VARCHAR(30) | 是 | upload | 视频来源，如 `profile_upload`、`post_video`、`upload` |
| analysis_id | VARCHAR(64) | 否 | NULL | 关联动作分析任务 ID，预留 |
| shot_type | VARCHAR(40) | 否 | NULL | 主要动作类型，如 forehand/backhand/serve/mixed，预留 |
| score | DOUBLE | 否 | NULL | 关联分析评分，预留 |
| visibility | VARCHAR(20) | 是 | private | 可见性：`private`、`public`、`friends` |
| status | VARCHAR(20) | 是 | uploaded | 状态：`uploaded`、`published`、`deleted` |
| like_count | INT | 是 | 0 | 点赞数 |
| comment_count | INT | 是 | 0 | 评论数 |
| favorite_count | INT | 是 | 0 | 收藏数 |
| view_count | INT | 是 | 0 | 播放/浏览数 |
| publish_time | DATETIME | 否 | NULL | 发布时间，公开后写入 |
| create_time | DATETIME | 是 | CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | 是 | CURRENT_TIMESTAMP | 更新时间 |

## 五、状态约定

### visibility

| 值 | 含义 | 前端展示位置 |
|---|---|---|
| private | 私密视频 | 仅上传者个人资料页可见 |
| public | 公开视频 | 上传者个人资料页可见，首页短视频流也可见 |
| friends | 好友可见 | 预留，当前暂不实现 |

### status

| 值 | 含义 | 说明 |
|---|---|---|
| uploaded | 已上传 | 已进入私人库，未公开 |
| published | 已发布 | 已公开，首页可见 |
| deleted | 已删除 | 所有页面不展示 |

首页查询条件固定为：

```sql
WHERE visibility = 'public'
  AND status = 'published'
ORDER BY publish_time DESC, create_time DESC
```

个人页查询条件为：

```sql
WHERE user_id = 当前用户
  AND status != 'deleted'
ORDER BY create_time DESC
```

## 六、完整建表 SQL

```sql
CREATE TABLE IF NOT EXISTS `training_video_record` (
  `id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `user_id` BIGINT NOT NULL COMMENT '用户ID',
  `title` VARCHAR(120) DEFAULT NULL COMMENT '视频标题',
  `description` TEXT DEFAULT NULL COMMENT '视频描述/发布文案',
  `video_url` VARCHAR(500) NOT NULL COMMENT '视频URL或存储路径',
  `cover_url` VARCHAR(500) DEFAULT NULL COMMENT '封面URL',
  `duration_seconds` DOUBLE DEFAULT NULL COMMENT '视频时长，秒',
  `file_size` BIGINT DEFAULT NULL COMMENT '文件大小，字节',
  `source_type` VARCHAR(30) NOT NULL DEFAULT 'upload' COMMENT '来源：camera/album/upload/analysis',
  `analysis_id` VARCHAR(64) DEFAULT NULL COMMENT '关联动作分析任务ID',
  `shot_type` VARCHAR(40) DEFAULT NULL COMMENT '主要动作类型：forehand/backhand/serve/mixed',
  `score` DOUBLE DEFAULT NULL COMMENT '关联分析评分，0-100',
  `visibility` VARCHAR(20) NOT NULL DEFAULT 'private' COMMENT '可见性：private/public/friends',
  `status` VARCHAR(20) NOT NULL DEFAULT 'uploaded' COMMENT '状态：uploaded/published/deleted',
  `like_count` INT NOT NULL DEFAULT 0 COMMENT '点赞数',
  `comment_count` INT NOT NULL DEFAULT 0 COMMENT '评论数',
  `favorite_count` INT NOT NULL DEFAULT 0 COMMENT '收藏数',
  `view_count` INT NOT NULL DEFAULT 0 COMMENT '播放/浏览数',
  `publish_time` DATETIME DEFAULT NULL COMMENT '发布时间',
  `create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_analysis_id` (`analysis_id`),
  KEY `idx_shot_type` (`shot_type`),
  KEY `idx_visibility_status` (`visibility`, `status`),
  KEY `idx_publish_time` (`publish_time`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='训练视频记录表';
```

## 七、增量迁移 SQL

如果服务器已经有旧版 `training_video_record`，执行以下迁移。注意：`ADD COLUMN IF NOT EXISTS` 需要 MySQL 8.0.29+。低版本 MySQL 请先 `SHOW COLUMNS`，再手动添加缺失字段。

```sql
ALTER TABLE `training_video_record`
  ADD COLUMN IF NOT EXISTS `description` TEXT DEFAULT NULL COMMENT '视频描述/发布文案' AFTER `title`,
  ADD COLUMN IF NOT EXISTS `visibility` VARCHAR(20) NOT NULL DEFAULT 'private' COMMENT '可见性：private/public/friends' AFTER `score`,
  ADD COLUMN IF NOT EXISTS `status` VARCHAR(20) NOT NULL DEFAULT 'uploaded' COMMENT '状态：uploaded/published/deleted' AFTER `visibility`,
  ADD COLUMN IF NOT EXISTS `like_count` INT NOT NULL DEFAULT 0 COMMENT '点赞数' AFTER `status`,
  ADD COLUMN IF NOT EXISTS `comment_count` INT NOT NULL DEFAULT 0 COMMENT '评论数' AFTER `like_count`,
  ADD COLUMN IF NOT EXISTS `favorite_count` INT NOT NULL DEFAULT 0 COMMENT '收藏数' AFTER `comment_count`,
  ADD COLUMN IF NOT EXISTS `view_count` INT NOT NULL DEFAULT 0 COMMENT '播放/浏览数' AFTER `favorite_count`,
  ADD COLUMN IF NOT EXISTS `publish_time` DATETIME DEFAULT NULL COMMENT '发布时间' AFTER `view_count`,
  ADD COLUMN IF NOT EXISTS `update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间' AFTER `create_time`;

CREATE INDEX `idx_training_video_visibility_status`
  ON `training_video_record` (`visibility`, `status`);

CREATE INDEX `idx_training_video_publish_time`
  ON `training_video_record` (`publish_time`);
```

## 八、后端接口说明

### 1. 上传视频到私人库

```text
POST /api/videos/upload
Content-Type: multipart/form-data
```

请求字段：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| file | File | 是 | 视频文件，字段名必须是 `file` |
| user_id | int | 否 | 用户 ID，默认 1 |
| title | string | 否 | 视频标题 |
| description | string | 否 | 视频描述/发布文案 |
| source_type | string | 否 | 来源，默认 upload |
| cover_url | string | 否 | 封面地址，当前可不传 |
| duration_seconds | float | 否 | 视频时长，当前可不传 |

支持的视频格式：

```text
.mp4 .mov .m4v .avi .webm
```

成功返回：

```json
{
  "code": 200,
  "message": "上传成功，已保存到私人库",
  "data": {
    "id": 1,
    "user_id": 1,
    "title": "今日训练",
    "description": "分享一次新的网球训练",
    "video_url": "/uploads/videos/20260708120000_xxx.mp4",
    "src": "/uploads/videos/20260708120000_xxx.mp4",
    "cover_url": null,
    "visibility": "private",
    "status": "uploaded",
    "type": "video",
    "create_time": "2026-07-08 12:00:00"
  }
}
```

错误返回：

```json
{
  "code": 400,
  "message": "视频文件不能为空",
  "data": null
}
```

### 2. 查询我的视频库

```text
GET /api/videos/my?user_id=1&page=1&page_size=20
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| user_id | int | 否 | 用户 ID，默认 1 |
| page | int | 否 | 页码，默认 1 |
| page_size | int | 否 | 每页数量，默认 20，最大 50 |

查询范围：

```text
当前用户全部未删除视频，包括 private 和 public
```

成功返回：

```json
{
  "code": 200,
  "message": "查询成功",
  "data": {
    "total": 2,
    "page": 1,
    "page_size": 20,
    "list": [
      {
        "id": 1,
        "user_id": 1,
        "title": "今日训练",
        "video_url": "/uploads/videos/xxx.mp4",
        "src": "/uploads/videos/xxx.mp4",
        "visibility": "private",
        "status": "uploaded",
        "create_time": "2026-07-08 12:00:00"
      }
    ]
  }
}
```

### 3. 发布公开视频

```text
POST /api/videos/{video_id}/publish
Content-Type: application/x-www-form-urlencoded
```

请求字段：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| user_id | int | 否 | 用户 ID，默认 1 |
| content | string | 否 | 发布文案，传入后会更新 description |

业务规则：

```text
只能发布当前 user_id 自己上传的视频
发布后 visibility=public
发布后 status=published
发布后 publish_time=当前时间
```

成功返回：

```json
{
  "code": 200,
  "message": "发布成功，公开视频流已可见",
  "data": {
    "id": 1,
    "user_id": 1,
    "title": "今日训练",
    "description": "今天正手训练很有感觉",
    "video_url": "/uploads/videos/xxx.mp4",
    "visibility": "public",
    "status": "published",
    "publish_time": "2026-07-08 12:10:00"
  }
}
```

失败返回：

```json
{
  "detail": "视频不存在或无权发布"
}
```

### 4. 设置为私密

```text
POST /api/videos/{video_id}/private
Content-Type: application/x-www-form-urlencoded
```

请求字段：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| user_id | int | 否 | 用户 ID，默认 1 |

业务规则：

```text
visibility=private
status=uploaded
```

该接口目前为预留接口，前端暂未重点使用。

### 5. 首页短视频流

```text
GET /api/feed/list?page=1&page_size=10
```

请求参数：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---:|---|
| page | int | 否 | 页码，默认 1 |
| page_size | int | 否 | 每页数量 |

数据库查询条件：

```text
visibility = public
status = published
```

成功返回：

```json
{
  "code": 200,
  "msg": "success",
  "message": "success",
  "total": 1,
  "page": 1,
  "page_size": 10,
  "data": [
    {
      "id": 1,
      "user_id": 1,
      "nickname": "网球训练者",
      "author": "@网球训练者",
      "avatar_url": null,
      "title": "今日训练",
      "desc": "今天正手训练很有感觉",
      "video_url": "/uploads/videos/xxx.mp4",
      "src": "/uploads/videos/xxx.mp4",
      "cover_url": null,
      "cover": null,
      "like_count": 0,
      "comment_count": 0,
      "favorite_count": 0,
      "view_count": 0,
      "publish_time": "2026-07-08 12:10:00",
      "type": "video",
      "source": "database"
    }
  ]
}
```

如果数据库无公开数据，接口仍返回 `code=200`，但 `data` 可能来自静态资源兜底或为空。

## 九、前端调用说明

前端统一封装文件：

```text
tennis0.7/tennis0.7/doubao_service（huilderx）/utils/video-api/index.js
```

封装方法：

| 方法 | 对应接口 | 用途 |
|---|---|---|
| uploadPrivateVideo | POST /api/videos/upload | 上传到私人库 |
| fetchMyVideos | GET /api/videos/my | 查询个人页视频 |
| publishVideo | POST /api/videos/{id}/publish | 发布公开 |
| fetchPublicFeed | GET /api/feed/list | 首页短视频流 |

当前前端接入页面：

```text
pages/content/post-video/post-video.vue
pages/tabbar/profile/profile.vue
pages/tabbar/index/index.vue
```

前端本地存储仍保留：

```text
utils/video-records/index.js
utils/community-posts/index.js
```

使用原则：

```text
后端可用：以后端数据为准
后端不可用：使用本地记录或演示数据兜底
```

## 十、联调顺序

建议按以下顺序联调：

### 1. 建表

如果没有表：

```text
执行 sql/training_video_record.sql
```

如果已有旧表：

```text
执行 sql/training_video_record_video_feed_migration.sql
```

### 2. 启动后端

确认后端可以访问：

```text
GET http://服务器IP:9000/health
GET http://服务器IP:9000/docs
```

### 3. 测试上传私人视频

使用 Postman、Apifox 或前端上传：

```text
POST http://服务器IP:9000/api/videos/upload
```

检查：

```text
1. uploads/videos/ 下是否出现视频文件
2. training_video_record 是否出现一条 private/uploaded 记录
3. 返回 video_url 是否能在浏览器打开
```

### 4. 测试我的视频库

```text
GET http://服务器IP:9000/api/videos/my?user_id=1
```

检查是否能返回刚才上传的视频。

### 5. 测试发布公开

```text
POST http://服务器IP:9000/api/videos/{video_id}/publish
```

检查数据库：

```text
visibility = public
status = published
publish_time 不为空
```

### 6. 测试首页 feed

```text
GET http://服务器IP:9000/api/feed/list?page=1&page_size=10
```

检查是否返回刚才发布的视频。

### 7. 前端真机测试

确认前端 `utils/video-api/index.js` 中的局域网 IP 是当前后端服务器 IP：

```js
const LAN_API_BASE_URL = 'http://192.168.1.53:9000'
```

手机和电脑需要连接同一个 Wi-Fi。

## 十一、注意事项

1. 不要把视频二进制直接存进数据库。
2. 数据库存储的是 `video_url`，文件本体存在服务器磁盘或对象存储。
3. 服务器进程需要有 `uploads/videos` 的写权限。
4. 手机真机不能访问电脑的 `127.0.0.1`，必须使用电脑局域网 IP 或公网域名。
5. 首页只展示 `visibility=public` 且 `status=published` 的视频。
6. 个人页展示当前用户自己的全部未删除视频。
7. 如果 MySQL 版本不支持 `ADD COLUMN IF NOT EXISTS`，迁移 SQL 需要手动拆分执行。
8. 当前用户体系暂时用 `user_id=1` 作为默认值，接入真实登录后需要替换为登录用户 ID。

## 十二、当前代码位置汇总

后端：

```text
TennisCoach（后端代码）/routers/videos.py
TennisCoach（后端代码）/routers/feed.py
TennisCoach（后端代码）/db_models/training_video_record.py
TennisCoach（后端代码）/sql/training_video_record.sql
TennisCoach（后端代码）/sql/training_video_record_video_feed_migration.sql
```

前端：

```text
doubao_service（huilderx）/utils/video-api/index.js
doubao_service（huilderx）/pages/content/post-video/post-video.vue
doubao_service（huilderx）/pages/tabbar/profile/profile.vue
doubao_service（huilderx）/pages/tabbar/index/index.vue
```

## 十三、给数据库同学的最短说明

请为短视频功能建表或补字段，核心表为 `training_video_record`。

核心展示逻辑：

```text
用户上传视频 -> 插入 private/uploaded 记录 -> 个人页可见
用户发布公开 -> 改为 public/published -> 首页 feed/list 可见
```

首页公开视频查询：

```sql
SELECT *
FROM training_video_record
WHERE visibility = 'public'
  AND status = 'published'
ORDER BY publish_time DESC, create_time DESC;
```

个人视频库查询：

```sql
SELECT *
FROM training_video_record
WHERE user_id = ?
  AND status != 'deleted'
ORDER BY create_time DESC;
```
